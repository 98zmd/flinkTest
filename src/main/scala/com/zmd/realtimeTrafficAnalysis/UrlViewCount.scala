package com.zmd.realtimeTrafficAnalysis

case class UrlViewCount(
                         url: String,
                         windowEnd: Long,
                         count: Long
                       )
